-- Data/Categories.lua
local DB = DynamicBar
DB.Data = DB.Data or {}
DB.Data.Categories = DB.Data.Categories or {}
local C = DB.Data.Categories

-- ordered highest priority first
C.Hearth = {
  6948, -- Hearthstone
}
